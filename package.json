{
	"name": "weather",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"generate": "npx -y swagger-typescript-api generate --no-client -p https://api.weather.gov/openapi.json -o ./src/weather-api -n weather-gov-api.ts",
		"test": "playwright test"
	},
	"dependencies": {
		"@playwright/test": "^1.54.2",
		"@preact/preset-vite": "^2.10.2",
		"@types/node": "^24.3.0",
		"preact": "^10.27.0",
		"sass": "^1.78.0",
		"typescript": "^5.5.4",
		"vite": "^7.1.4"
	}
}
