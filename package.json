{
	"name": "weather",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "tsc --noEmit && vite build",
		"preview": "vite preview",
		"generate": "npx -y swagger-typescript-api --no-client -p https://api.weather.gov/openapi.json -o ./src/spec -n weather-gov-api.ts",
		"test": "playwright test"
	},
	"dependencies": {
		"preact": "^10.27.0",
		"sass": "^1.78.0",
		"typescript": "^5.5.4",
		"vite": "^6.3.5",
		"@preact/preset-vite": "^2.10.2",
		"@playwright/test": "^1.45.1"
	}
}
